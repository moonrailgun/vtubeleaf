# 验收步骤与当前证据

日期：2026-09-07。此表区分已检查的本地工件与尚未验证的产品行为。编译、单元测试、模拟 UDP 数据不能证明真实面捕或会议可用。

## 当前资源与品牌检查

| 项目                       | 结果     | 证据与边界                                                                                                                                                                                                                                                        |
| -------------------------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 资源脚本本地自检           | 通过     | `node scripts/setup-assets.mjs --self-test`；覆盖哈希不匹配、大小限制、原子写入                                                                                                                                                                                   |
| MediaPipe 本地资源         | 通过     | `npm run setup:assets -- --mediapipe-only` 实际准备五个资源，再运行 `--check --mediapipe-only`，大小和 SHA-256 匹配                                                                                                                                               |
| Cubism Core                | 通过     | 官方 Cubism SDK for Web 5 R4 已下载、复制并通过 `npm run setup:assets -- --check`；来源与 SHA-256 见 `THIRD_PARTY.md`。R5 实际绘制失败，已定位为渲染接口变化并改用 R4                                                                                             |
| OpenSeeFace 安装脚本自检   | 通过     | `node scripts/setup-openseeface.mjs --self-test`；仅版本输入与固定来源检查                                                                                                                                                                                        |
| OpenSeeFace Python 环境    | 本机通过 | 实际安装隔离环境；Python 3.10.19、NumPy 1.26.4、OpenCV 4.11.0.86、Pillow 8.4.0、ONNX Runtime 1.23.2；`npm run setup:openseeface -- --check` 与 `python -m pip check` 通过，完整解析版本留在 `.local/openseeface-resolved.txt`                                     |
| OpenSeeFace 实际推理与 UDP | 本机通过 | 隔离 Python 环境运行 `scripts/check-openseeface.py .local/face-landmark-example.png`，由正式 `scripts/run-openseeface.py` 启动固定上游，收到 3 个有效单人数据包并回收子进程；未使用摄像头。默认 CoreML 路径两次在 45 秒内没有有效包，CPU 启动脚本下同一检查退出 0 |
| 品牌导出                   | 通过     | `node scripts/brand.mjs` 生成矢量、16–1024 PNG、ICO/ICNS；检查 PNG 尺寸和 RGBA、文件头                                                                                                                                                                            |
| 品牌文件视觉检查           | 已查看   | 查看三方案提案板及 128px 图标；16/32/64px 预览能辨认暂定图形。未在 Dock、开始菜单或 Linux 启动器实测                                                                                                                                                              |

## 代码与浏览器检查

执行环境：macOS 15.7.4、Apple Silicon arm64、Node.js 24.20.0、Rust 1.92.0。以下命令在本次工作区执行，均退出 0。

| 检查                              | 结果                   | 证据与边界                                                                                                                                                                                                                                                                                                                                                                     |
| --------------------------------- | ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| TypeScript 单元检查               | 14/14 通过             | `npm test`：不可信设置、自定义映射与参数边界、按模型保存/恢复、校准/丢脸、扩展 MediaPipe 输入、快捷键规范化/冲突及录制时限/导出结构                                                                                                                                                                                                                                            |
| Rust 单元及本机集成检查           | 16/16 通过             | `cargo test --manifest-path src-tauri/Cargo.toml`：路径/符号链接隔离、ZIP、设置原子保存、扩展 UDP 解析/接收、端口释放、自有进程退出/回收及动作导出校验；另有一项需要模型路径的检查默认跳过，已分别提供 Haru、Wanko 路径显式运行并通过                                                                                                                                          |
| Rust 格式                         | 通过                   | `cargo fmt --manifest-path src-tauri/Cargo.toml --check`                                                                                                                                                                                                                                                                                                                       |
| TypeScript/Vite 与 macOS 原生构建 | 通过                   | `npm run tauri build -- --bundles app` 包含 TypeScript 检查、Vite 生产构建及 Rust release；生成 `src-tauri/target/release/bundle/macos/VTubeLeaf.app`。包含官方 R4 Core，未启动原生验收                                                                                                                                                                                        |
| 浏览器回归                        | 16/16 通过             | `npm run build` 后同时设置 `VTUBELEAF_FACE_FIXTURE`、`VTUBELEAF_MODEL_FIXTURE`（Haru）和 `VTUBELEAF_ALT_MODEL_FIXTURE`（Wanko）运行 `npx playwright test`，16 项全部通过。覆盖生产 CSP、设置恢复、不自动采集、权限拒绝、输出、异步停止、OSF 错误、模型渲染、鼠标构图、面部网格、模型控制、全窗口舞台、直播模式与角色库拖入/预览/恢复。未提供对应图片或模型路径时会跳过相关检查 |
| Live2D 实际渲染                   | Haru 通过              | 官方 R4 Core + 同一 SDK 的 Haru；加载 moc3、纹理、物理和姿态资源，验证转头/闭眼/张嘴、组合表情、动作单次/循环/保持末帧与停止恢复。被动输出模型的最终参数和部件透明度与主模型一致，并查看中立/驱动/动作输出截图。Chromium 软件 WebGL、生产 CSP，文件读取使用测试 IPC；不代表原生文件导入或摄像头驱动通过                                                                        |
| 不同参数能力的模型                | Wanko 通过             | 同一 SDK 的 Wanko 使用 `PARAM_ANGLE_X` 等旧式参数名且不包含表情；确认不生成不存在的标准参数，通过自定义映射驱动头部，播放/停止自带动作后参数仍有限，查看实际渲染截图。Chromium 软件 WebGL，文件读取使用测试 IPC                                                                                                                                                                |
| 真实模型导入后端                  | Haru、Wanko 通过       | 对两个模型分别显式运行 `imports_supplied_model_and_reads_all_resources -- --ignored`：外部入口复制到角色库，库内文件和目录返回同一注册模型，ZIP 导入到独立目录，所有声明资源与原始文件逐字节一致。测试临时目录自动回收；没有操作原生选择对话框                                                                                                                                 |
| MediaPipe 实际推理                | 合成输入和人脸图片通过 | Chromium + 本地 WASM/模型；合成输入验证无脸回中立、暂停保留轨道、停止释放全部轨道，且未发生外部 HTTP 请求。使用 [官方示例图片](https://developers.google.com/static/mediapipe/images/solutions/examples/face_landmark.png) 的 canvas 视频流连续获得至少 3 次人脸结果，无错误，停止后轨道释放。未使用真实摄像头，不能证明动态姿态和延迟                                         |
| 界面视觉检查                      | 已查看                 | Chromium 软件 WebGL，1200×800 全窗口舞台、Haru 直播模式与 900×650 最小窗口截图；面板覆盖在舞台上，隐藏面板不改变构图。未作原生窗口验收                                                                                                                                                                                                                                         |
| npm 生产依赖审计                  | 0 项已知漏洞           | `npm audit --omit=dev`，仅代表此次审计结果                                                                                                                                                                                                                                                                                                                                     |

OpenSeeFace 退出回归先在未接入前端错误监听时失败，再加入监听后通过。自动眨眼覆盖动作保持的闭眼状态也已通过真实 Haru 回归复现并修复，检查同时覆盖闭眼表情。Rust 中的自有进程回收使用测试替身，只验证生命周期管理。

React / shadcn 迁移后重新通过 14 项逻辑测试、13 项完整浏览器回归和 Prettier 检查。新增回归覆盖开关/滑块键盘操作、状态刷新后的焦点与设置恢复，以及 React StrictMode 卸载时保存待写设置、释放延迟授权的摄像头。模型切换回归验证最近模型与各自映射正确恢复。生产 CSP 检查使用实际构建产物；没有放宽桌面安全策略。

鼠标构图与面捕预览更新后，通过 14 项逻辑测试和 14 项完整浏览器回归：真实 Haru 模型支持舞台左键拖动、滚轮缩放、保存构图与重置；人脸图片生成的视频流用于验证 MediaPipe 网格绘制、镜像对齐、暂停清空、恢复重绘、丢脸与停止清空。已查看角色拖动缩放与网格预览截图；未使用真实摄像头或操作原生窗口。首次完整运行有两项旧界面检查失败，未修改实现的单项与完整重跑均通过；当时存在文件编辑，尚未证明失败原因。

全窗口舞台与直播模式更新后，通过 15 项完整浏览器回归：画布始终等于窗口尺寸，收起面板或进入直播模式不替换画布、不改变构图；开启的摄像头预览被隐藏后，同一视频流仍持续前进，Esc 恢复预览与按钮焦点。检查也覆盖 Tab 激活 Pixi 辅助交互层后，恢复的面板仍可点击。已查看真实 Haru 的普通界面与直播画面，以及最小窗口截图，并重新运行 `npm run tauri build -- --bundles app` 生成新版 macOS 应用；未使用真实摄像头、操作原生窗口或验证 OBS 捕获。

角色库更新后，通过 14 项逻辑测试、16 项 Rust 检查及 16 项完整浏览器回归，并分别使用 Haru、Wanko 运行真实模型后端检查。后端覆盖文件和目录复制、删除测试源后继续读取、扫描恢复、坏目录隔离与预览持久化；浏览器通过模拟 Tauri 拖拽事件导入两个真实模型及一个无效路径，验证单项失败后继续、实际缩略图像素、卡片切换和重新打开后的恢复。已查看两个角色的预览卡片及最小窗口截图，重新生成 macOS 应用包。Finder 原生拖拽、原生退出重开及 OBS 捕获仍待实测。

2026-09-08 方形头像更新：10 项模型后端测试通过（另 1 项依赖外部模型的检查未运行），覆盖头像随文件/目录/ZIP 导入保留、重启读取、优先于生成缓存及符号链接越界防护。定向浏览器回归确认 JPEG icon 直接显示为方形且不读取模型资源；没有 icon 时，真实模型在尚未选中前自动生成头像，切换与刷新后仍可恢复。已查看 Haru、Wanko、Hiyori、Mao 的头像截图；此处使用 Chromium 软件 WebGL 和模拟 Tauri IPC，未操作原生窗口。

2026-09-08 上半身识别更新：16 项逻辑检查、20 项完整浏览器回归、TypeScript、Prettier 与本地运行资源哈希检查通过。逻辑检查覆盖肩膀及关节的可见性、出框和非有限值过滤，身体角度与头部独立、校准、镜像，以及丢失后的平滑回退。浏览器使用本地 Pose Landmarker Lite 对官方静态图片生成的视频流实际推理：完整上半身输出身体角度与抬臂值，裁去髋部后不输出身体俯仰，换成人脸近景后清空身体信号并继续面捕；关闭上半身识别不请求姿态模型，姿态资源返回 404 时面捕仍正常，停止后释放视频轨道。Haru 的 `ParamBodyAngleX` 实际渲染检查通过，已查看金色身体关键点、面捕面板和角色截图。姿态图片的侧脸未通过人脸检测，所以身体推理和人脸回退分别检查，不代表同时识别或真实摄像头动态跟随通过。原生摄像头、持续运行性能及遮挡恢复效果仍待实测。

此次 `npm run tauri build -- --bundles app` 已通过，并重新生成 macOS 应用包；打包前构建产物中的姿态模型哈希与固定资源一致。

## 当前磁盘体积

2026-09-07 本机 `du -sk` 快照，单位换算为 MiB；这是当前文件占用，不是完整发行包预算。应用包含 R4 Core，示例模型保留在开发目录中。

| 内容                        |  本机占用 | 范围                              |
| --------------------------- | --------: | --------------------------------- |
| macOS `.app`                |  18.2 MiB | 本次 release 构建，未制作 DMG     |
| 本地运行资源                |  25.3 MiB | 开发目录中的 MediaPipe 与 R4 Core |
| 可选 OpenSeeFace 源码与模型 | 165.0 MiB | 包含固定源码检出的 Git 元数据     |
| 可选 Python 虚拟环境        | 315.3 MiB | 本机安装的跟踪依赖                |
| 可选 Python 3.10 运行时     |  58.4 MiB | 本次隔离安装，不随应用包提供      |

## 功能验收矩阵

| 项目             | 操作与通过条件                                                                                                 | 当前状态                                                                    |
| ---------------- | -------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------- |
| 实际安装包启动   | 从打包产物安装启动；记录系统版本、CPU 架构、WebView 版本及签名状态                                             | 未验证                                                                      |
| 摄像头权限       | 首次允许/拒绝、系统撤销权限后重试；错误可恢复                                                                  | 未验证                                                                      |
| 摄像头生命周期   | 开始、暂停、停止、切换、拔出、占用、关闭应用；停止后系统占用指示释放                                           | 未验证                                                                      |
| 真实模型         | 至少一个许可明确的模型，记录 Core/模型版本；头眼嘴跟随，缺参模型可降级                                         | Haru 参数驱动及 Wanko 缺参/自定义映射浏览器检查通过；原生与摄像头跟随待验证 |
| 导入恢复         | 文件/目录/ZIP 各一例；缺资源、重复入口、超限或越界模型有具体错误                                               | Haru、Wanko 三种入口的后端检查通过；原生选择对话框待验证                    |
| 角色库           | 拖入多个目录/入口/ZIP 后复制保存，预览可见，点击切换，原文件移动后仍可用                                       | Rust 复制/恢复及真实模型浏览器预览通过；Finder 原生拖拽待验证               |
| 映射与校准       | 中立姿态、左右转头、上下点头、倾斜、单眼闭合、嘴部闭合；参数不越界                                             | 未验证设备行为                                                              |
| 按模型配置       | 切换两个模型后恢复各自的映射、校准、构图、待机和快捷键                                                         | 状态单元检查和浏览器映射恢复通过；原生退出重开待验证                        |
| 表情与动作       | 叠加/关闭表情，播放单次/循环/保持末帧动作，停止后恢复跟踪；输出同步                                            | Haru 浏览器检查通过；真实摄像头下组合操作待验证                             |
| 全局快捷键       | 前台、后台触发表情和动作，重复/冲突组合提示，切换模型释放旧绑定                                                | 规范化和注册请求检查通过；系统后台触发待验证                                |
| 录制导出         | 手动录制、停止、取消保存和保存；到达 60 秒停止，导出文件可读取                                                 | 时限/结构单元检查、浏览器导出请求通过；原生保存对话框待验证                 |
| 低质环境         | 低光、眼镜、距离变化、大角度转头；记录抖动和误识别                                                             | 未验证                                                                      |
| 丢脸恢复         | 遮脸/离开后按设置回中立，重新进入后恢复，不停在夸张表情                                                        | 未验证                                                                      |
| 设置恢复         | 正常退出再打开；模型被移动、设置损坏能恢复；不自动开始摄像头                                                   | 未验证原生应用                                                              |
| 干净输出         | 主窗口直播模式或独立输出只含角色与背景，无设置、状态或摄像头预览；Esc 可恢复主窗口界面，独立输出关闭再开仍正常 | 浏览器检查通过；原生窗口与 OBS 捕获待验证                                   |
| 引擎切换         | MediaPipe ↔ OpenSeeFace；同一时刻只有选定引擎占用摄像头，托管进程可回收                                        | 未验证                                                                      |
| 外部 OpenSeeFace | 本机端口一致可接收；无数据、坏包、停止、外部进程仍运行有明确状态                                               | 真实进程推理与 UDP 脚本链路通过；原生应用接收待验证                         |
| 后台运行         | 飞书在前台、窗口被遮挡、最小化分别记录跟踪/绘制帧率，不笼统合并结论                                            | 未验证                                                                      |
| 离线与隐私       | 准备资源后断网运行；确认无正常运行时下载、原始帧或面部轨迹日志                                                 | 未验证实际运行                                                              |
| OBS → 飞书       | 第二会议端看到持续的角色画面，麦克风正常；记录捕获源和虚拟摄像头版本                                           | 未验证                                                                      |
| 长会议           | 连续 2 小时，记录中断次数、峰值内存、CPU、温度和可观察延迟                                                     | 未验证                                                                      |
| 键盘与可访问性   | Tab 可达、焦点可见、控件有名称、状态变化可理解                                                                 | Chromium 开关、滑块及导航键盘回归通过；原生待验证                           |
| Windows / Linux  | 在每个平台重复摄像头、模型、后台和会议链路测试                                                                 | 未验证                                                                      |

## 可复用记录

每次实测单独保留记录，至少包含：

```text
日期 / 测试者：
提交与工作区修改：
操作系统 / CPU / 内存 / WebView：
开发启动还是安装包 / 签名状态：
摄像头型号 / 分辨率：
跟踪引擎与版本 / Cubism Core 版本与哈希：
模型名称与版本 / 授权来源：
OBS 版本 / 捕获源 / 飞书版本 / 第二端设备：
窗口状态 / 持续时长：
步骤 / 期望 / 实际结果：
CPU / 峰值内存 / 跟踪和渲染频率 / 延迟测量方法：
截图或录像路径（经同意，不录原始人脸作为默认日志）：
通过项 / 失败项 / 未覆盖项：
```

当前不承诺最低系统版本、性能预算、两小时稳定性或所有 Cubism 模型兼容。只有实际证据覆盖的平台和路径，才适合在发行说明中标为可用。
