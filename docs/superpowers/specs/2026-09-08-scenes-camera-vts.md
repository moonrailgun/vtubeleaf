# Scenes, items, native camera and VTS configuration

User request: complete items/scenes, add a built-in virtual camera including macOS, expand global shortcuts, and import VTube Studio `.vtube.json` mappings and shortcuts. Explain NDI; NDI implementation is outside this request.

Use the existing PIXI canvas, model library, settings persistence and native global-shortcut plugin. Add local PNG/JPEG/GIF items (including animated GIF), background images and reusable Live2D items; support position, scale, rotation, opacity, front/back ordering, visibility, locking and model-relative attachment. Scenes capture the selected model, composition, background and items and can be saved, updated, renamed, deleted and recalled with shortcuts. Scene recall validates assets before replacing the current composition. Output window and camera render the same scene.

Import VTS configuration explicitly into the active model using verified real field names; match parameter and animation/expression filenames, validate all data, and report unsupported fields/actions instead of silently claiming complete compatibility. Keep local settings editable afterwards. Expand global actions to tracking, pause, calibration, microphone, model visibility/reset, output, virtual camera, scenes and items. Never enable camera/microphone automatically on startup.

macOS uses Apple's CMIO Camera Extension, a bundled native host bridge and a supported install/enable UI. Feed only the composed avatar canvas over a bounded local transport. Default 1280x720 at 30 FPS; stop clears output and releases resources. System activation requires a signed app and explicit user installation. Compile without a signing identity for development and document a reproducible signed packaging path; do not claim a usable installed camera without system-level verification. Other platforms retain the existing OBS output workflow and expose an honest unsupported state.

Preserve existing dirty work; do not commit, push, deploy, launch/control native GUI or access physical camera/microphone. No certificate selection without the user's team information. No SIP bypass or legacy DAL installation. Input paths and settings are untrusted; apply size/type/path bounds and deny arbitrary file reads. Reuse dependencies and leave focused runnable checks plus browser integration evidence. Existing 37 logic, 24 browser and app build coverage is the baseline.
